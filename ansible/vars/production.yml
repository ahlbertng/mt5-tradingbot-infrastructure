---
# Non-secret production configuration
# Safe to store in version control — secrets go in vars/secrets.yml (ansible-vault encrypted)

environment: production
cloud_provider: oracle

aws_region: us-east-1
cloudwatch_log_group: /aws/ec2/mt5-trading-bot

db_username: trading_admin
db_name: trading_db
